datasource db {
  provider = "sqlite"
  url      = "file:./dev.bd"
}

generator client {
  provider = "prisma-client-js"
}

model todo {
  id   Int     @id @default(autoincrement())
  text String
  mark Boolean @default(false)
}
